<resources>

    <style name="Dialog_BG1" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@drawable/chat_dialog_bg1</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <style name="Dialog_BG2" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@drawable/chat_dialog_bg2</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

</resources>